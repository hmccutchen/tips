









 $('.vote-like-<%=@review.id %>').text('<%=@review.get_upvotes.size %>' )


$('.tlik-<%=@review.id %>').html('<%= j (link_to "tips", unlike_page_path(@review.id), method: :put, remote: true)%>')

console.log("liked!")

//this is from the unlike file if this isnt working its safe to delete
$('.vote-dislike-<%=@review.id %>').text('<%=@review.get_upvotes.size %>' )
$('.tdis-<%=@review.id %>').html('<%= j (link_to "tip", like_page_path(@review.id), method: :put, remote: true)%>')

//maybe if i can use ajax on the actual links as well,
//changeing the paths i might be able to toggle likes and unlikes?














